# 张瑞个人作品集 — 二次交接文档

> 生成时间：2026-09-14
> 交接原因：本轮新增「项目现场 / FIELDWORK」照片墙后，用户对展示效果不满意，决定转交给下一个 AI 继续。
> 前置阅读：先读同目录的 `PROJECT_HANDOFF.md`（上一轮对项目整体、视觉方向和红线的完整说明）和 `design-qa.md`（含本次增补记录）。本文是「本轮改动 + 当前状态 + 待解决问题」的增量说明。

## 1. 本轮做了什么

用户提供了一批活动/演讲/项目照片素材，并亲自按 10 个分类整理到目录：

```text
/Users/raelzhang/Desktop/日常项目/个人简历/个人网站素材/
├── AI课程设计/
├── 企业决策圈运营峰会沙龙活动/
├── 小红书运营1/  小红书运营2/  小红书运营3/  小红书运营4/
├── 校园实验室建设/
├── 短片剪辑/         ← 4 段视频（原始，每段约 22–25MB）
├── 腾讯龙虾区域增长专项目/
└── 讲师分享/
```

基于此，在页面「个人能力（Capabilities）」之后、「联系方式（Contact）」之前，新增了一个区块：

- 区块 id：`field`，标题 `FIELDWORK`，副标题 `项目现场`，导航序号 `04`。
- 左侧是模块列表（10 个模块），右侧是大图轮播。
- 图片模块：约 4.5 秒自动切下一张；悬停或聚焦时暂停；左右箭头、底部圆点可手动切换；`prefers-reduced-motion` 下关闭自动播放。
- 视频模块（短片剪辑）：抽封面 + 点击播放，不自动播放、不自动切换。

### 素材处理

- 62 张图片统一压成 WebP，最长边 ≤ 1600px，总约 14MB，输出到 `public/gallery/<模块key>/NN.webp`。
- 4 段视频用 ffmpeg 压成 H.264（宽度 ≤ 1280、CRF 26、AAC 128k），各约 1.6–2.6MB，并各抽一帧海报，输出到 `public/gallery/video/`（`NN.mp4` + `poster-NN.jpg`）。

### 新增 / 修改的文件

新增：

```text
src/gallery.js                      模块数据（key / 中文标题 / 数量 / 类型）
src/components/FieldGallery.jsx     画廊组件
src/components/FieldGallery.css     画廊样式
public/gallery/                     处理后的全部图片与视频（70 个文件）
```

修改：

```text
src/main.jsx                        引入 FieldGallery、新增 Fieldwork 区块并挂入 App
design-qa.md                        追加「FIELDWORK gallery」增补记录
```

## 2. 当前状态（已核实）

- `npm run build` 通过（5042 个模块），无报错。
- 开发服务器 `npm run dev` 可运行，默认 http://localhost:5173/ 。
- 图片、视频、海报在 dev 下均返回 200。
- 本仓库**没有任何 git remote**，且工作区处于脏状态：上一轮迭代 + 本轮的改动全部未提交（见第 5 节）。切勿执行 `git reset --hard`、`git checkout -- .` 或大范围清理。

## 3. 用户不满意的核心问题（重点）

照片比例差异大（横版、竖版、方形、超长图混杂），用户反馈「很多照片没有完整显示、只显示一半」。

我上一版把图片用固定 16:10 舞台 + `object-fit: contain` 展示，竖图/方图会被缩小居中、四周留大片黑边，视觉上像「没显示全」。随后我改成「模糊铺底 + 完整显示」：

- 背景：同一张图放大、模糊（`blur(26px)`）、压暗后铺满整个画框（`object-fit: cover`）。
- 前景：原图按比例完整显示（`max-width/max-height: 100%`）。

但这只是缓解，用户仍评价整体效果不佳并决定转交。下一个 AI 接手时应**优先重新审视照片墙的展示方案**，而不是继续在这个实现上微调。可能的替换方向：

1. 每张照片按原始比例展示（画框高度随当前照片自适应），而非塞进固定比例画框。
2. 点击放大看原图的 lightbox，主区域只用统一尺寸缩略图。
3. 错落网格（masonry），放弃轮播、让所有照片同时按原比例呈现。
4. 若坚持轮播，至少要让横/竖/方图在视觉上等权、不出现大片黑边或过小的一条。

## 4. 内容真值与红线（沿用上一轮，不可违反）

- 真实资料：姓名张瑞、手机 186 0936 6869、邮箱 2256178941@qq.com、三个项目及其链接、量化指标 700万+ / 9K+ / 5万+ / 100万+。不得擅自改写、四舍五入或补充地址/学校/时间/薪资等未出现的信息。
- 无证件照：`portrait.webp` 已删除，不得恢复。
- 只保留 `TargetCursor`，不要恢复 `SplashCursor`；不要同时启用两种鼠标动效。
- 模块中文标题当前**照抄文件夹名**，其中「腾讯龙虾区域增长专项目」疑似「专项」笔误，小红书运营 1–4 也只是编号，尚未确认正式命名——改动前建议与用户确认。
- 保留第三套电影感视觉方向，不要把页面改回渐变卡片式 SaaS 模板。

## 5. 工作区与 git 注意事项

`git status --short` 显示大量 `M` / `D` / `??`，全部是用户当前成果，包括 `dist/` 的旧哈希删除与新哈希新增、`public/portrait.webp` 的删除等。继续开发前先 `git status --short`，只改与当前需求直接相关的文件；不要清理、不要恢复已删除文件、不要把代码清理与视觉修改混在一起。

## 6. 建议接手步骤

1. 完整读 `PROJECT_HANDOFF.md`、`design-qa.md`、本文件，再 `git status --short`。
2. 与用户确认照片墙的目标形态（固定轮播 / 自适应比例 / 网格 / lightbox），以及模块标题是否要改。
3. 重新实现或替换 `src/components/FieldGallery.jsx` 与 `FieldGallery.css`（数据在 `src/gallery.js`，可复用）。
4. 完成后 `npm run build`，并在桌面（1440×1024）与移动端（390px）验收：横/竖/方图都能完整可读、无横向滚动、控制台无 warning/error。
5. 视觉有变化时同步更新 `design-qa.md`。

## 7. 验收清单（沿用上一轮并针对照片墙补充）

- `npm run build` 无错误。
- Hero 完整、`700万+` 单行、`SELECTED WORK` 空格正确。
- 照片墙：切换模块与前后翻页正常；自动轮播正常、悬停暂停；三种比例照片都完整、无裁切、无大片黑边。
- 视频模块：封面显示、点击可播放。
- 390px 宽度无横向滚动；控制台无新增 warning/error。
